
archinstall

```
Mirror: Australia
Disk configuration: ext4
Bootloader: systemd-boot
Authentication : new user
Profile: Hyprland, nvidia (open kernel), ly (greeter)
Applications: bluetooth: enabled, audio: pipewire
Network: Use network manager (default backend)


# yay
sudo pacman -S --needed git base-devel && git clone https://aur.archlinux.org/yay.git && cd yay && makepkg -si


#  clipboard
yay -S cliphist


# term
yay -S \
  wget \
  curl \
  htop \
  neovim \
  gcc \
  cmake \
  npm \
  man-db \
  fd \
  ripgrep \
  fzf \
  jq \
  uv \ 
  zsh \
  lnav \
  lazydocker \
  lazygit \
  clang

  
# hyprland
yay -S \
  hyprsunset \
  hyprpaper \
  brightnessctl \ 
  rofi \
  rofi-calc \
  swww \
  hyprpolkitagent \
  hyprshot \
  waybar \
  blueman \
  pavucontrol \
  xdg-desktop-portal-hyprland # screensharing

# video
yay -S \
  ffmpeg \
  mediainfo \
  mediainfo-gui \
  mpv

# work
yay -S \
  docker \
  docker-compose \
  networkmanager-openvpn \
  nm-connection-editor \
  nasm \
  gst-rtsp-server \
  nvidia-container-toolkit

# desktop
yay -S \
  vivaldi \
  obsidian \
  spotify \
  nemo \
  nemo-fileroller


systemctl --user enable --now ssh-agent

# turn off wifi powersave (is potentially causing dropouts)
# i had to also pin wifi band

/etc/NetworkManager/conf.d/wifi-powersave-off.conf 
```
sudo vim /etc/NetworkManager/conf.d/default-wifi-powersave-on.conf
```

  
todo:
- ssh config

