# clipboard
```
sudo pacman -S cliphist
```

then added to hyprland config:
```
exec-once = wl-paste --type text --watch cliphist store # Stores only text data
exec-once = wl-paste --type image --watch cliphist store # Stores only image data

```


# term
yay -S \
  git \
  wget \
  curl \
  htop \
  neovim \
  gcc \
  cmake \
  npm \
  man-db \
  fd \
  ripgrep \
  fzf \
  jq \
  uv \ 
  zsh \
  lnav \
  lazydocker \
  lazygit \
  clang

  
# hyprland
yay -S \
  hyprsunset \
  hyprpaper \
  brightnessctl \ 
  rofi \
  rofi-calc \
  swww \
  hyprpolkitagent \
  hyprshot \
  waybar \
  blueman \
  pavucontrol \
  xdg-desktop-portal-hyprland # screensharing

# video
yay -S \
  ffmpeg \
  mediainfo \
  mediainfo-gui \
  mpv

# work
yay -S \
  docker \
  docker-compose \
  networkmanager-openvpn \
  nm-connection-editor \
  nasm \
  gst-rtsp-server \
  nvidia-container-toolkit

# desktop
yay -S \
  vivaldi \
  obsidian \
  spotify \
  nemo \
  nemo-fileroller


systemctl --user enable --now ssh-agent

turn off wifi powersave (is potentially causing dropouts)

/etc/NetworkManager/conf.d/wifi-powersave-off.conf 
```
sudo vim /etc/NetworkManager/conf.d/default-wifi-powersave-on.conf
```
  
todo:
- fix screenshare for window
- hookup hyprshot to hyprpanel button
  - hyprshot -m region -o "$outputDir" -f "$outputFile"
  - hyprshot -m output --clipboard-only 
- google-cloud-cli?? 
- ssh config
- turn off hover to foucus on window, must click

